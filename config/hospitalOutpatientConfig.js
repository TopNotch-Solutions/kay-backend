'use strict';

/** Kay One stub — hospital outpatient modules are not used. */
module.exports = {
  HOSPITAL_OUTPATIENT_DEPARTMENTS: {},
  departmentLabel: (key) => key || '—',
  departmentForRole: () => null,
};
